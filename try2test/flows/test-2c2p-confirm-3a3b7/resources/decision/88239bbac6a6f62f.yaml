processor_resources:
  - label: "{package}_88239bbac6a6f62f"
    hp_transform:
      name: "Check Error"
      result_mode: merge
      mapping: |-
        root = this

        meta result_route = match {
        tokenData.error.or("") != "" => "this.{package}_a732c5da004f8675",
        _ => "this.{package}_e6ee3592fd23dccd",
        }

