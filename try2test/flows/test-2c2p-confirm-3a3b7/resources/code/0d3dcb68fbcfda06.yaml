processor_resources:
  - label: "{package}_0d3dcb68fbcfda06"
    hp_transform:
      name: "Prepare Fail"
      input: |-
            root = this
      mapping: |-
            root = this
            root.billerRef = {}
            root.status.status = this.code
            
            root.request_response = {
                "response": this.result,
                "request": this.request,
                "action": this.action,
            }
            
      result_mode: merge
      output: |-
            root = this
