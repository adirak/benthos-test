processor_resources:
  - label: "{package}_1ac645501b9e5057"
    hp_redis_cmd:
      profile: |-
        {{hyper_ibank_redis_config}}
        
      command: set
      args_mapping: root = [ this.redis_key, this.strValue, "EX", this.expire.number(0).ceil().string().or("0") ]
      input: |
        root = this
    
      result_mode: "void"
    
      output: |
        root = {}
    