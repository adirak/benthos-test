processor_resources:
  - label: "{package}_a9a63d6758ade139"
    hp_transform:
      name: "Compile Config"
      input: |-
            root = this
      mapping: |-
            root = this
            let strConfig = {{hyper_ibank_secret_config}}
            
            root.config = $strConfig
                
      result_mode: merge
      output: |-
            root = this
