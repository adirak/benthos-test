processor_resources:
  - label: "{package}_df314e01a67df662"
    hp_redis_cmd:
      profile: |-
        {{hyper_ibank_redis_config}}
        
      command: set
      args_mapping: root = [ this.redis_key, this.strValue ]
      input: |
        root = this
    
      result_mode: "void"
    
      output: |
        root = {}
    