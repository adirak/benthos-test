
processor_resources:
  - label: "{package}_4019add936594cf8"
    hp_transform:
      name: "Define In/Out"
      input: |-
        root = this
      mapping: |-
        root = this
      result_mode: merge
      output: |-
        let filter = model2json("1ce814de46f542cf_4019add936594cf8_in")
        root = this.json_filter($filter)
