
processor_resources:
  - label: "{package}_799cc71f6ea2e304"
    hp_transform:
      name: "Define In/Out"
      input: |-
        root = this
      mapping: |-
        root = this
      result_mode: merge
      output: |-
        let filter = model2json("1ce814de46f542cf_799cc71f6ea2e304_in")
        root = this.json_filter($filter)
