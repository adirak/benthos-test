
processor_resources:
  - label: "{package}_f484c9742a0db45e"
    hp_transform:
      name: "Define In/Out"
      input: |-
        root = this
      mapping: |-
        root = this
      result_mode: merge
      output: |-
        let filter = model2json("1ce814de46f542cf_f484c9742a0db45e_in")
        root = this.json_filter($filter)
