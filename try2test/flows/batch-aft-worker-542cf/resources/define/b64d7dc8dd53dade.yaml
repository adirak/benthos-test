
processor_resources:
  - label: "{package}_b64d7dc8dd53dade"
    hp_transform:
      name: "Define In/Out"
      input: |-
        root = this
      mapping: |-
        root = this
      result_mode: merge
      output: |-
        let filter = model2json("1ce814de46f542cf_b64d7dc8dd53dade_in")
        root = this.json_filter($filter)
