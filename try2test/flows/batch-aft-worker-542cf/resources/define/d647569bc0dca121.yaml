
processor_resources:
  - label: "{package}_d647569bc0dca121"
    hp_transform:
      name: "Define Input / Output"
      input: |-
        root = this
      mapping: |-
        root = this
      result_mode: merge
      output: |-
        let filter = model2json("1ce814de46f542cf_d647569bc0dca121_in")
        root = this.json_filter($filter)
