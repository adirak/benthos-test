
processor_resources:
  - label: "{package}_cbbc34fe63061eff"
    hp_transform:
      name: "Define In/Out"
      input: |-
        root = this
      mapping: |-
        root = this
      result_mode: merge
      output: |-
        let filter = model2json("1ce814de46f542cf_cbbc34fe63061eff_in")
        root = this.json_filter($filter)
