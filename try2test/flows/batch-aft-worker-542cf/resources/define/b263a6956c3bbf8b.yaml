
processor_resources:
  - label: "{package}_b263a6956c3bbf8b"
    hp_transform:
      name: "Define In/Out"
      input: |-
        root = this
      mapping: |-
        root = this
      result_mode: merge
      output: |-
        let filter = model2json("1ce814de46f542cf_b263a6956c3bbf8b_in")
        root = this.json_filter($filter)
