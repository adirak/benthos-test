
processor_resources:
  - label: "{package}_66998b8eefac0cd2"
    hp_transform:
      name: "Define In/Out"
      input: |-
        root = this
      mapping: |-
        root = this
      result_mode: merge
      output: |-
        let filter = model2json("1ce814de46f542cf_66998b8eefac0cd2_in")
        root = this.json_filter($filter)
