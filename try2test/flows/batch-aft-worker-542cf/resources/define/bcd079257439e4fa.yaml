
processor_resources:
  - label: "{package}_bcd079257439e4fa"
    hp_transform:
      name: "Define In/Out"
      input: |-
        root = this
      mapping: |-
        root = this
      result_mode: merge
      output: |-
        let filter = model2json("1ce814de46f542cf_bcd079257439e4fa_in")
        root = this.json_filter($filter)
