
processor_resources:
  - label: "{package}_4435aba8fae0ecce"
    hp_transform:
      name: "Define Input / Output"
      input: |-
        root = this
      mapping: |-
        root = this
      result_mode: merge
      output: |-
        let filter = model2json("1ce814de46f542cf_4435aba8fae0ecce_in")
        root = this.json_filter($filter)
