
processor_resources:
  - label: "{package}_b253ccced633bab0"
    hp_transform:
      name: "Define Input"
      input: |-
        root = this
      mapping: |-
        root = this
      result_mode: merge
      output: |-
        let filter = model2json("1ce814de46f542cf_b253ccced633bab0_in")
        root = this.json_filter($filter)
