
processor_resources:
  - label: "{package}_744c30ab2224795b"
    hp_transform:
      name: "Define In/Out"
      input: |-
        root = this
      mapping: |-
        root = this
      result_mode: merge
      output: |-
        let filter = model2json("1ce814de46f542cf_744c30ab2224795b_in")
        root = this.json_filter($filter)
