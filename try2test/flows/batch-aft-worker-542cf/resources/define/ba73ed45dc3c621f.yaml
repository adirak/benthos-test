
processor_resources:
  - label: "{package}_ba73ed45dc3c621f"
    hp_transform:
      name: "Define In/Out"
      input: |-
        root = this
      mapping: |-
        root = this
      result_mode: merge
      output: |-
        let filter = model2json("1ce814de46f542cf_ba73ed45dc3c621f_in")
        root = this.json_filter($filter)
