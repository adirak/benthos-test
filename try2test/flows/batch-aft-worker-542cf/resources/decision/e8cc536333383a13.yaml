processor_resources:
  - label: "{package}_e8cc536333383a13"
    hp_transform:
      name: "Check Not Found Or Empty"
      result_mode: merge
      mapping: |-
        root = this

        meta result_route = match {
        not_found || results.length() == 0 => "this.{package}_823d8a22f97cc833",
        _ => "this.{package}_90c3d6ad0b0773ef",
        }

