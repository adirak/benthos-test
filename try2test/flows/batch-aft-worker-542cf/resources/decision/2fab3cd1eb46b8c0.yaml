processor_resources:
  - label: "{package}_2fab3cd1eb46b8c0"
    hp_transform:
      name: "Check no trans_type"
      result_mode: merge
      mapping: |-
        root = this

        meta result_route = match {
        trans_type == "" || trans_type == null => "this.{package}_88253c190a699a14",
        _ => "this.{package}_5897d3500d1e2fda",
        }

