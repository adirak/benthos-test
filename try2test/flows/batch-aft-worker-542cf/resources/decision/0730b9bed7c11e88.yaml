processor_resources:
  - label: "{package}_0730b9bed7c11e88"
    hp_transform:
      name: "Check Debtor Creditor Acct"
      result_mode: merge
      mapping: |-
        root = this

        meta result_route = match {
        this.trans.creditor.info.promptpay.toAcctNo.number(0).floor() == this.trans.debtor.AccountID.number(0).floor() => "this.{package}_789d646a7052cc46",
        _ => "this.{package}_0518b2c2a922414f",
        }

