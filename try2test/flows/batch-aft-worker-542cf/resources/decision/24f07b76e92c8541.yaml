processor_resources:
  - label: "{package}_24f07b76e92c8541"
    hp_transform:
      name: "Check Not Empty"
      result_mode: merge
      mapping: |-
        root = this

        meta result_route = match {
        this.trnObj.length().or(0) > 0 => "this.{package}_cb817a0636beb5e3",
        _ => "this.{package}_190e63dbcadd9a3f",
        }

