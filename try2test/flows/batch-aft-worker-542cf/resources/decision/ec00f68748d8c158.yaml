processor_resources:
  - label: "{package}_ec00f68748d8c158"
    hp_transform:
      name: "Check Code"
      result_mode: merge
      mapping: |-
        root = this

        meta result_route = match {
        code != 0 || post_status != 0 => "this.{package}_2e92b72b476475e5",
        _ => "this.{package}_bf11fcaf691d242c",
        }

