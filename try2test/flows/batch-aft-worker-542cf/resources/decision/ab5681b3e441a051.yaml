processor_resources:
  - label: "{package}_ab5681b3e441a051"
    hp_transform:
      name: "Check dr_acct"
      result_mode: merge
      mapping: |-
        root = this

        meta result_route = match {
        dr_acct != 0 && drCode != 0 => "this.{package}_65ad6ed4d166b0c2",
        _ => "this.{package}_68e986da68449b13",
        }

