processor_resources:
  - label: "{package}_5bbd19ed6619b98e"
    hp_transform:
      name: "Check Value in Cache"
      result_mode: merge
      mapping: |-
        root = this

        meta result_route = match {
        cid != null && cid !="" && !not_found && result.accountNumber == cid => "this.{package}_f0b7663707218cf6",
        _ => "this.{package}_4e5c92ef03684b13",
        }

