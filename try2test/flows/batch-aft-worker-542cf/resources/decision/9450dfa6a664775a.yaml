processor_resources:
  - label: "{package}_9450dfa6a664775a"
    hp_transform:
      name: "Check Stop Account"
      result_mode: merge
      mapping: |-
        root = this

        meta result_route = match {
        acc_no == trans.debtor.AccountID => "this.{package}_cc36306a9a8f3004",
        acc_no == trans.creditor.AccountID && (restrict == 11 || restrict == 13) => "this.{package}_cc36306a9a8f3004",
        _ => "this.{package}_863137a975d10119",
        }

