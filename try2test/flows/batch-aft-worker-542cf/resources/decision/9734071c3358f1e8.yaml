processor_resources:
  - label: "{package}_9734071c3358f1e8"
    hp_transform:
      name: "Check Empty"
      result_mode: merge
      mapping: |-
        root = this

        meta result_route = match {
        user_trans != null && user_trans.length() > 0 => "this.{package}_2df9fa88093ab132",
        _ => "this.{package}_1c7d3df55c0bbdee",
        }

