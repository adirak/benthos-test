processor_resources:
  - label: "{package}_0307d1b00bb617ca"
    hp_transform:
      name: "Check Not Found"
      result_mode: merge
      mapping: |-
        root = this

        meta result_route = match {
        not_found => "this.{package}_974cc27c4e8cd003",
        _ => "this.{package}_ab670a3bc2b2729a",
        }

