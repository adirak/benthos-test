processor_resources:
  - label: "{package}_667a1e1fda9cf80b"
    hp_transform:
      name: "Check Status"
      result_mode: merge
      mapping: |-
        root = this

        meta result_route = match {
        result.status == 1 || result.status == 5 || prod_cfg.dr_trans == null || prod_cfg.dr_trans == 0 => "this.{package}_cf5d3841c13a1d9f",
        _ => "this.{package}_ce0e4c25f8874ec8",
        }

