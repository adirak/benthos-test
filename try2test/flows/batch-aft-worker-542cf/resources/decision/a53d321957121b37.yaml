processor_resources:
  - label: "{package}_a53d321957121b37"
    hp_transform:
      name: "Check dr_cif restrict"
      result_mode: merge
      mapping: |-
        root = this

        meta result_route = match {
        root.cus_rest.get(cr_cif.string()) != null && root.cus_rest.get(cr_cif.string()) != "" => "this.{package}_86068ee2c86851cc",
        _ => "this.{package}_2d004e20fb1b2648",
        }

