processor_resources:
  - label: "{package}_5e49a2a08ab5125c"
    hp_transform:
      name: "Check Cutoff time"
      result_mode: merge
      mapping: |-
        root = this

        meta result_route = match {
        this.trans_cfg.cutoff_time == null || this.trans_cfg.cutoff_time.length().or(0) == 0 => "this.{package}_86e22d91d45d5131",
        _ => "this.{package}_98abd47cf399afd6",
        }

