processor_resources:
  - label: "{package}_a82a3c32d946e84f"
    hp_transform:
      name: "Check Code"
      result_mode: merge
      mapping: |-
        root = this

        meta result_route = match {
        code > 0 => "this.{package}_b3c554255a0b1f35",
        _ => "this.{package}_9bb56c29723c7d2d",
        }

