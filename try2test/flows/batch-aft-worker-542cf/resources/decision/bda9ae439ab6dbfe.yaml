processor_resources:
  - label: "{package}_bda9ae439ab6dbfe"
    hp_transform:
      name: "Check dr_cif restrict"
      result_mode: merge
      mapping: |-
        root = this

        meta result_route = match {
        root.cus_rest.get(dr_cif.string()) != null && root.cus_rest.get(dr_cif.string()) != "" => "this.{package}_c8eb1eebcdae2f67",
        _ => "this.{package}_a53d321957121b37",
        }

