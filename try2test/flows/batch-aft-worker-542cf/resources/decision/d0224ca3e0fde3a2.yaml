processor_resources:
  - label: "{package}_d0224ca3e0fde3a2"
    hp_transform:
      name: "Check Old Data"
      result_mode: merge
      mapping: |-
        root = this

        meta result_route = match {
        this.oldData != null && this.oldData.length().or(0) > 0 => "this.{package}_d729b46c68c0229f",
        _ => "this.{package}_4fb24cc28adc6a67",
        }

