processor_resources:
  - label: "{package}_40e159d0ba168db1"
    hp_transform:
      name: "Check Value in Cache"
      result_mode: merge
      mapping: |-
        root = this

        meta result_route = match {
        cid != null && cid !="" && !not_found && result.accountNumber == cid => "this.{package}_a3023b7201d1203c",
        _ => "this.{package}_effcab1492915e2a",
        }

