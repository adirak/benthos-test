processor_resources:
  - label: "{package}_d26508c4c4e39d50"
    hp_transform:
      name: "Check Stop Account"
      result_mode: merge
      mapping: |-
        root = this

        meta result_route = match {
        acct_info.stop_acct == null || acct_info.stop_acct != 0 => "this.{package}_c6f0ebc29d721a24",
        _ => "this.{package}_6ded1db6d2612fc2",
        }

