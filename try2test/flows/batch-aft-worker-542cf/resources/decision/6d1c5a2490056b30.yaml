processor_resources:
  - label: "{package}_6d1c5a2490056b30"
    hp_transform:
      name: "Check Status"
      result_mode: merge
      mapping: |-
        root = this

        meta result_route = match {
        result.status == 1 || result.status == 5 || prod_cfg.cr_trans == null || prod_cfg.cr_trans == 0 => "this.{package}_8c409ef240bf778a",
        _ => "this.{package}_2ce345648a036e54",
        }

