processor_resources:
  - label: "{package}_0da17934380abe56"
    hp_transform:
      name: "hp_transform"
      input: |
        root = this
    
      mapping: |
        root = this
    
      result_mode: merge
    
      output: |
        root = this
        root.data.mobAction = this.action
        root.data.mobCode = this.mob_code
        root.data.mobDescription = this.mob_message
    