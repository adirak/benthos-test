processor_resources:
  - label: "{package}_795618d94f87a19c"
    hp_transform:
      name: "hp_transform"
      input: |
          root = this
          
      mapping: |
          root = {}
          
          root.encryptedRequest = if this.reqBody.length() > 0 {
                this.reqBody.gcm_encode(this.eKey.string(), this.reqBody.string(), -1).or("Error")
          } else {
              ""
          }
          
    
    
      result_mode: merge      