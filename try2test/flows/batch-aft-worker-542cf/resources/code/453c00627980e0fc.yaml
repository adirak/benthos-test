processor_resources:
  - label: "{package}_453c00627980e0fc"
    hp_transform:
      name: "Init Loop"
      input: |-
            root = this
      mapping: |-
            root = this
            root.index = 0
            root.size = this.jobs.length().or(0)
            root.sTime = timestamp_unix_milli()
      result_mode: merge
      output: |-
            root = this
