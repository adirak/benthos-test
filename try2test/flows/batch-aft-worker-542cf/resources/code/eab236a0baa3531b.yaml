processor_resources:
  - label: "{package}_eab236a0baa3531b"
    hp_transform:
      name: "Prepare KTB Action"
      input: |-
            root = this
      mapping: |-
            root = this
            root.action = "cancel"
      result_mode: merge
      output: |-
            root = this
