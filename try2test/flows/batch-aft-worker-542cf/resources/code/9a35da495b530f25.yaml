processor_resources:
  - label: "{package}_9a35da495b530f25"
    hp_transform:
      name: "Split to Array"
      input: |-
            root = this
      mapping: |-
            root = this
            root.results = this.hits.data.split("|")
            root.result = this.hits.data
      result_mode: merge
      output: |-
            root = this
