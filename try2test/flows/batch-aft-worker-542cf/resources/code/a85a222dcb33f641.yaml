processor_resources:
  - label: "{package}_a85a222dcb33f641"
    hp_transform:
      name: "Prepare to Read"
      input: |-
            root = this
      mapping: |-
            root = this
            root.aftID = this.job.aftID
      result_mode: merge
      output: |-
            root = this
