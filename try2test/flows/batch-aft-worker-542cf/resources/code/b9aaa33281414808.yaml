processor_resources:
  - label: "{package}_b9aaa33281414808"
    hp_transform:
      name: "Prepare Result"
      input: |-
            root = this
      mapping: |-
            root = this
            root.acct_info.stop_acct = this.code
            root.acct_info.acct_list = this.results
      result_mode: merge
      output: |-
            root = this
