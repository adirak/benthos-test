processor_resources:
  - label: "{package}_7e1a498fcf37c32f"
    hp_transform:
      name: "Prepare to Check"
      input: |-
            root = this
      mapping: |-
            root = this
            root.restrict = this.result.restriction.number(0).ceil()
            root.acc_no = this.result.accountNumber
      result_mode: merge
      output: |-
            root = this
