processor_resources:
  - label: "{package}_c6fa28316403ea40"
    hp_transform:
      name: "Prepare Data"
      input: |-
            root = this
      mapping: |-
            root = this
            root.xtrans.promptpay.toAcctNo = this.prepare.trans.creditor.info.promptpay.toAcctNo
            root.xtrans.promptpay.toAcctName = this.prepare.trans.creditor.info.promptpay.toAcctName
            root.xtrans.promptpay.toAcctDispName = this.prepare.trans.creditor.info.promptpay.toAcctDispName
            root.xtrans.promptpay.toTaxId = this.prepare.trans.creditor.info.promptpay.toTaxId
            
            
            
      result_mode: merge
      output: |-
            root = this
