processor_resources:
  - label: "{package}_cd2611e2a88f3551"
    hp_transform:
      name: "Init Loop"
      input: |-
            root = this
      mapping: |-
            root = this
            root.index = 0
            root.size = this.trn_dtl.length().or(0)
      result_mode: merge
      output: |-
            root = this
