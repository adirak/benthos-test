processor_resources:
  - label: "{package}_d3e5b55694bd6576"
    hp_transform:
      name: "Make Key cr_acct"
      input: |-
            root = this
      mapping: |-
            root = this
            root.redis_key = "cbs_deposit_" + "%d".format(cr_acct)
            
      result_mode: merge
      output: |-
            root = this
