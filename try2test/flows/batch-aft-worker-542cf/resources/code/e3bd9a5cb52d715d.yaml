processor_resources:
  - label: "{package}_e3bd9a5cb52d715d"
    hp_transform:
      name: "Prepare for Trans Header"
      input: |-
            root = this
      mapping: |-
            root = this
            root.count = this.trn_dtl.length().or(2)
            root.user = this.trans.user.or({})
            
            
      result_mode: merge
      output: |-
            root = this
