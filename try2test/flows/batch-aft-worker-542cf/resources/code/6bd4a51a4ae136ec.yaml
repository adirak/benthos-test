processor_resources:
  - label: "{package}_6bd4a51a4ae136ec"
    hp_transform:
        name: "hp_transform"
        input: |
          root = this
        
        mapping: |
          root = this
          
          root.data._id = deleted()
          root.data.logID = deleted()
          
          root.data.status = this.status.status
          root.data.description = this.status.description
        
        result_mode: new
        
        output: |
          root = this