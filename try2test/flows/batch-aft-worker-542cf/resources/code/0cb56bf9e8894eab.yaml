processor_resources:
  - label: "{package}_0cb56bf9e8894eab"
    hp_transform:
      name: "Prepare to CBS"
      input: |-
            root = this
      mapping: |-
            root = this
            root.trans.pre_post.detail = this.trn_dtl_prep
            
            root.trans.pre_post.message = this.trans.pre_post.header + "|" + this.trn_dtl_str.join("|")
            
            root.detail = this.trn_dtl_str
            root.header = this.trn_hdr.join("|")
            
            
      result_mode: merge
      output: |-
            root = this
