processor_resources:
  - label: "{package}_3b4f55df04f25df8"
    hp_transform:
      name: "Get by productType"
      input: |-
            root = this
      mapping: |-
            root = this
            root.prod_cfg = json("result." + this.productType.string())
      result_mode: merge
      output: |-
            root = this
