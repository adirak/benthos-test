processor_resources:
  - label: "{package}_4e1eeca9d36923eb"
    hp_transform:
      name: "Prepare Request"
      input: |-
            root = this
      mapping: |-
            root = this
            root.action = "ppconfirm"
            
            root.requestData.promptpay = this.xtrans.promptpay
            root.requestData.transaction = this.xtrans.transaction
      result_mode: merge
      output: |-
            root = this
