processor_resources:
  - label: "{package}_09f3e3659917e5a1"
    hp_transform:
      name: "Prepare Result"
      input: |-
            root = this
      mapping: |-
            root = this
            root.trn_dtl = [
                    this.trn_dtl0,
                    this.results
                ]
            
      result_mode: merge
      output: |-
            root = this
