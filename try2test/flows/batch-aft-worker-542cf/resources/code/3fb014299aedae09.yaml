processor_resources:
  - label: "{package}_3fb014299aedae09"
    hp_transform:
      name: "Prepare Detail"
      input: |-
            root = this
      mapping: |-
            root = this
            root.dtl = this.dtlRow.split("|")
            
      result_mode: merge
      output: |-
            root = this
