processor_resources:
  - label: "{package}_23575d19f1b39d6f"
    hp_redis_cmd:
      profile: {{hyper_ibank_redis_config}}
      command: get
      args_mapping: root = [ this.redis_key ]
      input: |
        root = this
    
      result_mode: "merge"
    
      output: |
        root = {}
        root.strConfig = this.result.or("{}")