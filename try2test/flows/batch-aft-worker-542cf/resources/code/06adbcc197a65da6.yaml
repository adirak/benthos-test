processor_resources:
  - label: "{package}_06adbcc197a65da6"
    hp_transform:
      name: "Init Loop"
      input: |-
            root = this
      mapping: |-
            root = this
            root.size = this.tKeys.length()
            root.index = 0
            root.endLoop = false
            root.code = 0
            
            root.payment_time = if this.payment_time == null {
                this.trans_time
            }
      result_mode: merge
      output: |-
            root = this
