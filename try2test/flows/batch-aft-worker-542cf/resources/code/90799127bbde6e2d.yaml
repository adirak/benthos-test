processor_resources:
  - label: "{package}_90799127bbde6e2d"
    hp_transform:
      name: "Prepare Result"
      input: |-
            root = this
      mapping: |-
            root = this
            root.trn_dtl = [
                    this.trn_dtl0,
                    this.results
                ]
            
      result_mode: merge
      output: |-
            root = this
