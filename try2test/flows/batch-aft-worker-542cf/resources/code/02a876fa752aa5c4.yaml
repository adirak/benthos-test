processor_resources:
  - label: "{package}_02a876fa752aa5c4"
    hp_transform:
      name: "Set Owner"
      input: |-
            root = this
      mapping: |-
            root = this
            root.trans.owner = this.trans.prepare.trans.owner
            
            root.redis_key = "session_cbs_trans_api_onus_" + this.ttx_id
      result_mode: merge
      output: |-
            root = this
