processor_resources:
  - label: "{package}_1c7d3df55c0bbdee"
    hp_transform:
      name: "Set Empty for user_trans"
      input: |-
            root = this
      mapping: |-
            root = this
            user_trans = {}
            not_found = true
      result_mode: merge
      output: |-
            root = this
