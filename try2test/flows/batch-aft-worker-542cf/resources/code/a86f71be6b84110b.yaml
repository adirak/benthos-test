processor_resources:
  - label: "{package}_a86f71be6b84110b"
    hp_transform:
      name: "Set Detail to TransPost"
      input: |-
            root = this
      mapping: |-
            root = this
            let detail = this.trn_dtl
            root.trans_post.detail = $detail
            
            
            root.header = this.trans_post.header
            root.detail = this.trn_dtl
            root.arrayType = true
      result_mode: merge
      output: |-
            root = this
