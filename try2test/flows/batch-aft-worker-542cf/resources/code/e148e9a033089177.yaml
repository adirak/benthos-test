processor_resources:
  - label: "{package}_e148e9a033089177"
    hp_transform:
      name: "Prepare to Check"
      input: |-
            root = this
      mapping: |-
            root = this
            root.dr_acct = this.trans.debtor.AccountID
            root.cr_acct = this.trans.creditor.AccountID
      result_mode: merge
      output: |-
            root = this
