processor_resources:
  - label: "{package}_29006522722736a0"
    hp_transform:
      name: "Prepare Trans Ref"
      input: |-
            root = this
      mapping: |-
            root = this
            root.redis_key = "cbs_trans_ref_onus_" + this.trans.transRef
            root.redis_value = this.ttx_id
            root.in_cache_value = true
            root.expire = 432000
            
      result_mode: merge
      output: |-
            root = this
