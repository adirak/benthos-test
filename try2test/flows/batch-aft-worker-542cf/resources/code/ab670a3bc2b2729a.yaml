processor_resources:
  - label: "{package}_ab670a3bc2b2729a"
    hp_transform:
      name: "hp_transform"
      input: |
        root = this
    
      mapping: |
        root = this
        root.data = this.aft
        root.aftID = this.aft.aftID
    
      result_mode: merge
    
      output: |
        root = this
        root.status.description = "start aft process"