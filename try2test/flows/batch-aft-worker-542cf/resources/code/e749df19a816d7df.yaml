processor_resources:
  - label: "{package}_e749df19a816d7df"
    hp_transform:
      name: "Prepare CID and CIF"
      input: |-
            root = this
      mapping: |-
            root = this
            cid = result.accountNumber
            cif = result.customerID
            productType = result.productType
            
      result_mode: merge
      output: |-
            root = this
