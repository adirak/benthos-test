processor_resources:
  - label: "{package}_c7d9ac3395f07480"
    hp_transform:
      name: "Get Account and Amt"
      input: |-
            root = this
      mapping: |-
            root = this
            root.account = this.transInput.AccountID.number(0).ceil().or(0)
            root.amt = this.ttxInput.amt
            
      result_mode: merge
      output: |-
            root = this
