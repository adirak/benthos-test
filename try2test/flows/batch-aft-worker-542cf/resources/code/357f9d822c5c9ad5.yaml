processor_resources:
  - label: "{package}_357f9d822c5c9ad5"
    hp_transform:
      name: "Get Transcode Data"
      input: |-
            root = this
      mapping: |-
            root = this
            let jsonPath = "result."+this.chanel.string()+"."+this.restict.string()+ "."+this.kind.string()+"."+this.trans_type.string()
            root.jsonPath = $jsonPath
            root.trnObj = json($jsonPath)
      result_mode: merge
      output: |-
            root = this
