processor_resources:
  - label: "{package}_b05d4699ca4ab8ce"
    hp_transform:
      name: "Prepare Get CIF"
      input: |-
            root = this
      mapping: |-
            root = this
            root.cif = this.aft.customerID.number(0).int64()
      result_mode: merge
      output: |-
            root = this
