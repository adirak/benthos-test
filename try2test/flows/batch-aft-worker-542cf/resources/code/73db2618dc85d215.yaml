processor_resources:
  - label: "{package}_73db2618dc85d215"
    hp_transform:
      name: "Set Owner"
      input: |-
            root = this
      mapping: |-
            root = this
            root.trans.owner = if this.trans.debtor.customerID == this.trans.creditor.customerID {
                1
            }
      result_mode: merge
      output: |-
            root = this
