processor_resources:
  - label: "{package}_cb817a0636beb5e3"
    hp_transform:
      name: "Convert to Json"
      input: |-
            root = this
      mapping: |-
            root = this
            root.trncde = this.trnObj.parse_json().or({})
      result_mode: merge
      output: |-
            root = this
