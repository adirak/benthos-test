processor_resources:
  - label: "{package}_97dc1022a08ba1d9"
    hp_transform:
      name: "Prepare Key"
      input: |-
            root = this
      mapping: |-
            root = this
            root.redis_key = "cbs_deposit_" + this.trans.debtor.AccountID.number(0).ceil().string()
      result_mode: merge
      output: |-
            root = this
