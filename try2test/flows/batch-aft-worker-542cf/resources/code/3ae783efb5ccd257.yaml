processor_resources:
  - label: "{package}_3ae783efb5ccd257"
    hp_transform:
      name: "Prepare Trans Code"
      input: |-
            root = this
      mapping: |-
            root = this
            root.trans_type = if this.trans_type == null || this.trans_type == "" {
                "TRANOFFUS"
            }
      result_mode: merge
      output: |-
            root = this
