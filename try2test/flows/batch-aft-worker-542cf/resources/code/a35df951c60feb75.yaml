processor_resources:
  - label: "{package}_a35df951c60feb75"
    hp_transform:
      name: "Make Key dr_acct"
      input: |-
            root = this
      mapping: |-
            root = this
            root.redis_key = "cbs_deposit_" + "%d".format(dr_acct)
            
            
            root.cid = this.results.index(index).cid
            root.bal = this.results.index(index).bal
            root.balavl = this.results.index(index).balavl
      result_mode: merge
      output: |-
            root = this
