processor_resources:
  - label: "{package}_e51ce253feb3b151"
    hp_transform:
      name: "Prepare Response"
      input: |-
            root = this
      mapping: |-
            root = this
            root.not_found = if this.cus_rest.length().or(0) > 0 {
                    false
                } else {
                    true
                }
      result_mode: merge
      output: |-
            root = this
