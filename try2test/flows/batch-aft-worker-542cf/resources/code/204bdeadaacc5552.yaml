processor_resources:
  - label: "{package}_204bdeadaacc5552"
    hp_transform:
      name: "Prepare Trans Api"
      input: |-
            root = this
      mapping: |-
            root = this
            root.chanel = "MOBL"
            root.restict = this.prod_cfg.rstgrp
            root.kind = 1
            root.trans_type = "TRANONUS"
      result_mode: merge
      output: |-
            root = this
