processor_resources:
  - label: "{package}_cedc424fe00a1e35"
    hp_transform:
      name: "Prepare KTB Action"
      input: |-
            root = this
      mapping: |-
            root = this
            root.action = if this.action != null && this.action != "" {
                this.action
            } else {
                "start"
            }
      result_mode: merge
      output: |-
            root = this
