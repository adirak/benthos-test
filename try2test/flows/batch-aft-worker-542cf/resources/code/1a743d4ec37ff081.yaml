processor_resources:
  - label: "{package}_1a743d4ec37ff081"
    hp_transform:
      name: "Set Action and Request Data"
      input: |-
            root = this
      mapping: |-
            root = this
            root.action = "end"
            root.data = this.aft
      result_mode: merge
      output: |-
            root = this
