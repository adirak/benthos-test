processor_resources:
  - label: "{package}_3baddd7890dc870f"
    hp_transform:
      name: "Prepare Amount"
      input: |-
            root = this
      mapping: |-
            root = this
            root.ttxAmtStr = "%.2f".format(this.ttx.amt.float64())
            root.ttxAmtInfoStr = "%.2f".format(this.ttx_info.amt.float64())
      result_mode: merge
      output: |-
            root = this
