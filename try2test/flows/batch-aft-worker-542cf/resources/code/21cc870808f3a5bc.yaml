processor_resources:
  - label: "{package}_21cc870808f3a5bc"
    hp_transform:
      name: "Prepare Result"
      input: |-
            root = this
      mapping: |-
            root = this
            root.result = if this.responseData != null {
                this.responseData
            } else {
                {}
            }
      result_mode: merge
      output: |-
            root = this
