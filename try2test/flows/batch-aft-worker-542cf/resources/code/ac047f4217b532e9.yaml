processor_resources:
  - label: "{package}_ac047f4217b532e9"
    hp_transform:
      name: "Set Header to Prepost"
      input: |-
            root = this
      mapping: |-
            root = this
            let headerStr = this.trn_hdr.join("|")
            
            root.trans.pre_post.header = $headerStr
      result_mode: merge
      output: |-
            root = this
