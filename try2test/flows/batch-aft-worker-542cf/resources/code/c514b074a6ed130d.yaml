processor_resources:
  - label: "{package}_c514b074a6ed130d"
    hp_transform:
      name: "Rename Value"
      input: |-
            root = this
      mapping: |-
            root = this
            root.redis_value = this.result
            
            root.expire = 0
            root.in_cache_value = true
      result_mode: merge
      output: |-
            root = this
