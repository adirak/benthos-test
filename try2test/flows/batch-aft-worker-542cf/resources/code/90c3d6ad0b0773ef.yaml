processor_resources:
  - label: "{package}_90c3d6ad0b0773ef"
    hp_transform:
      name: "Init Loop"
      input: |-
            root = this
      mapping: |-
            root = this
            root.index = 0
            root.size = results.length()
            
            root.cur_date = timestamp_unix_milli()
            
      result_mode: merge
      output: |-
            root = this
