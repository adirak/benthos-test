processor_resources:
  - label: "{package}_17d090ce19def3fd"
    hp_transform:
      name: "Rename Value"
      input: |-
            root = this
      mapping: |-
            root = this
            root.redis_value = this.result
            
            root.expire = 0
            root.in_cache_value = true
      result_mode: merge
      output: |-
            root = this
