processor_resources:
  - label: "{package}_fb0441680378bb44"
    hp_transform:
      name: "Get CID"
      input: |-
            root = this
      mapping: |-
            root = this
            root.cid = this.result.accountNumber
            
      result_mode: merge
      output: |-
            root = this
