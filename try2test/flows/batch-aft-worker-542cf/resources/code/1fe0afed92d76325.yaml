processor_resources:
  - label: "{package}_1fe0afed92d76325"
    hp_transform:
      name: "Prepare for Mongo"
      input: |-
            root = this
      mapping: |-
            root = this
            root.ttx_data.status = this.status
            root.ttx_data.trans = this.trans
            root.ttx_data.ttxID = this.ttx_id
            
            root.ttx_data.effDate = this.effDate
            root.ttx_data.header = this.header
            root.ttx_data.transDt = this.transDt
            
            root.collection = "ttx_xchange"
      result_mode: merge
      output: |-
            root = this
