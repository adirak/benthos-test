processor_resources:
  - label: "{package}_a77ccd08eba40646"
    hp_transform:
      name: "Set Code 2300"
      input: |-
            root = this
      mapping: |-
            root = this
            root.code = this.code
            root.post_status = this.code
            
      result_mode: merge
      output: |-
            root = this
