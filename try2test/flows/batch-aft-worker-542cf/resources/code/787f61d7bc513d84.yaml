processor_resources:
  - label: "{package}_787f61d7bc513d84"
    hp_transform:
      name: "Not Own Inst"
      input: |-
            root = this
      mapping: |-
            root = this
            root.trans.debtor.trans_comment = "XTRANS_DR"
            
            root.chanel = "MOBL"
            root.restict = "GLS"
            root.kind = -1
            
            
            root.trans_type = if this.trans_type == null || this.trans_type == "" {
                "TRANOFFUS"
            } 
            
      result_mode: merge
      output: |-
            root = this
