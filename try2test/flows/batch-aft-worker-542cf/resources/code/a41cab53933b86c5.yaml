processor_resources:
  - label: "{package}_a41cab53933b86c5"
    hp_transform:
      name: "Prepare XTRANS_CR"
      input: |-
            root = this
      mapping: |-
            root = this
            root.acct = this.trans.creditor.Account
            root.accname = this.trans.prepare.trans.creditor.AccountName
            root.prxtype = this.trans.creditor.ProxyType
            root.inst = this.trans.creditor.Inst
            
      result_mode: merge
      output: |-
            root = this
