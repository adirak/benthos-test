processor_resources:
  - label: "{package}_1566fff24513ea16"
    hp_transform:
      name: "Set Action and Request Data"
      input: |-
            root = this
      mapping: |-
            root = this
            root.action = "cancel"
            root.data = this.aft
      result_mode: merge
      output: |-
            root = this
