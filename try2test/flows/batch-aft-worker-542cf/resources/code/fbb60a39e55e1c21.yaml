processor_resources:
  - label: "{package}_fbb60a39e55e1c21"
    hp_transform:
      name: "Set Balance"
      input: |-
            root = this
      mapping: |-
            root = this
            root.result.balance = bal
            root.result.available = balavl
      result_mode: merge
      output: |-
            root = this
