processor_resources:
  - label: "{package}_71c57eb3a6b50c7d"
    hp_transform:
      name: "Make Fix SQL"
      input: |-
            root = this
      mapping: |-
            root = this
            root.redis_key = "query_statement_TRANSPOSTAFTER"
            root.redis_value = {
                    "sqlcmd": " select cid,acn,bal,balavl,zpriv from dep where cid in ({}) ",
                    "refield": ""
            }
            
            root.expire = 0
            root.in_cache_value = true
            
            
            root.sql_key = "TRANSPOSTAFTER"
            root.arrayType = true
            root.conditions = ["#{cid}"]
            root.params = {"cid": [this.dr_acct, this.cr_acct]}
            root.mockupName = "TRANSPOSTAFTER"
      result_mode: merge
      output: |-
            root = this
