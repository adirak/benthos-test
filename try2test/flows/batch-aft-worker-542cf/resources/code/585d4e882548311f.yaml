processor_resources:
  - label: "{package}_585d4e882548311f"
    hp_transform:
      name: "Make Redis Key"
      input: |-
            root = this
      mapping: |-
            root = this
            root.redis_key = "session_cbs_trans_api_offus_" + this.ttx_id
      result_mode: merge
      output: |-
            root = this
