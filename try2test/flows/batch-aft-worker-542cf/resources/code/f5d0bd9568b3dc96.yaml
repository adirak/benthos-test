processor_resources:
  - label: "{package}_f5d0bd9568b3dc96"
    hp_transform:
      name: "Set Status Code"
      input: |-
            root = this
      mapping: |-
            root = this
            root.status.status = this.code
            
            root.message = "Error at validate confirm"
      result_mode: merge
      output: |-
            root = this
