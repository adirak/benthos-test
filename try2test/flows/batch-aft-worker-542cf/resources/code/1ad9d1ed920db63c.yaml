processor_resources:
  - label: "{package}_1ad9d1ed920db63c"
    hp_transform:
      name: "Add Stop Code "
      input: |-
            root = this
      mapping: |-
            root = this
            let stpObj = { this.acct : this.stp }
            root.stop_account = this.stop_account.merge($stpObj)
      result_mode: merge
      output: |-
            root = this
