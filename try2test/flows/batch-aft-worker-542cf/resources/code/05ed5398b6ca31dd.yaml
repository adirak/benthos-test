processor_resources:
  - label: "{package}_05ed5398b6ca31dd"
    hp_transform:
      name: "Convert to Json"
      inputs: |-
          root = this
      mapping: |-
          root = this
          root.trans_cfg = this.strConfig.parse_json().or({})
      result_mode: new
      output: |-
          root = this