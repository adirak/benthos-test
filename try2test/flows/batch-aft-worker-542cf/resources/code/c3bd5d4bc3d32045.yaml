processor_resources:
  - label: "{package}_c3bd5d4bc3d32045"
    hp_transform:
      name: "Move to cache_value"
      input: |-
            root = this
      mapping: |-
            root = this
            let cache_value = { "cache_value": this.redis_value}
            
            root.strValue = $cache_value.string()
            
            
      result_mode: merge
      output: |-
            root = this
