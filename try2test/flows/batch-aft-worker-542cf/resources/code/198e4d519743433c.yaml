processor_resources:
  - label: "{package}_198e4d519743433c"
    hp_transform:
      name: "Output Array"
      input: |-
            root = this
      mapping: |-
            root = this
            root.results = this.hits.data
      result_mode: merge
      output: |-
            root = this
