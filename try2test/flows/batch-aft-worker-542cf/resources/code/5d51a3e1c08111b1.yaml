processor_resources:
  - label: "{package}_5d51a3e1c08111b1"
    hp_transform:
      name: "Set Action"
      input: |-
            root = this
      mapping: |-
            root = this
            root.action = "ppinquiry"
            
            root.requestData = {
                "promptpay": this.xtrans.promptpay,
                "transaction": this.xtrans.transaction
            }
      result_mode: merge
      output: |-
            root = this
