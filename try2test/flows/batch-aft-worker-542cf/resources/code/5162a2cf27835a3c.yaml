processor_resources:
  - label: "{package}_5162a2cf27835a3c"
    hp_transform:
      name: "Rename Value"
      input: |-
            root = this
      mapping: |-
            root = this
            root.redis_value = this.result
            
            root.expire = 0
            root.in_cache_value = true
      result_mode: merge
      output: |-
            root = this
