processor_resources:
  - label: "{package}_adbc557d6212c6d2"
    hp_transform:
      name: "Init Loop"
      input: |-
            root = this
      mapping: |-
            root = this
            root.index = 0
            root.size = this.trn_dtl.length().or(0)
            root.oldValue = "$$PSTDT$$"
            
            
      result_mode: merge
      output: |-
            root = this
