processor_resources:
  - label: "{package}_cfbb0444a1a105a3"
    hp_transform:
      name: "Set Back to Header"
      input: |-
            root = this
      mapping: |-
            root = this
            root.trans.prepare.trans.pre_post.header = this.trn_hdr.join("|")
            
      result_mode: merge
      output: |-
            root = this
