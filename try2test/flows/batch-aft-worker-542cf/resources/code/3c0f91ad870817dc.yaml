processor_resources:
  - label: "{package}_3c0f91ad870817dc"
    hp_transform:
      name: "Init Loop"
      input: |-
            root = this
      mapping: |-
            root = this
            root.size = cif.length()
            root.index = 0
            
            let cNow = now()
            root.cur_date = $cNow.ts_format("20060102").number()
      result_mode: merge
      output: |-
            root = this
