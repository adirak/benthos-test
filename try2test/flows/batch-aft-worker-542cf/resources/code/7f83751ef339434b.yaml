processor_resources:
  - label: "{package}_7f83751ef339434b"
    hp_transform:
      name: "To Int"
      input: |-
            root = this
      mapping: |-
            root = this
            root.month = this.monthStr.int64().or(0)
            root.year = this.yearStr.int64().or(0)
      result_mode: merge
      output: |-
            root = this
