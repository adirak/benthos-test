processor_resources:
  - label: "{package}_fdbeb200fdab8fcc"
    hp_transform:
      name: "Update index"
      input: |-
            root = this
      mapping: |-
            root = this
            root.index = this.index + 1
      result_mode: merge
      output: |-
            root = this
