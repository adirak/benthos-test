processor_resources:
  - label: "{package}_c9f46f1c6cc33288"
    hp_transform:
      name: "Set Trans Code"
      input: |-
            root = this
      mapping: |-
            root = this
            root.trans.debtor.transCode = trncde
            root.trans.user = user_trans
      result_mode: merge
      output: |-
            root = this
