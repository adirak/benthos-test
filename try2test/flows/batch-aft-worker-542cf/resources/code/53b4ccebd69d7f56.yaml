processor_resources:
  - label: "{package}_53b4ccebd69d7f56"
    hp_transform:
      name: "Check Trans Type"
      input: |-
            root = this
      mapping: |-
            root = this
            root.isOnus = if this.trans.creditor.Inst == "066" {
                true
            } else {
                false
            }
      result_mode: merge
      output: |-
            root = this
