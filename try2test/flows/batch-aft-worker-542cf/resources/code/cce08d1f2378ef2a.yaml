processor_resources:
  - label: "{package}_cce08d1f2378ef2a"
    hp_transform:
      name: "Set senderType"
      input: |-
            root = this
      mapping: |-
            root = this
            root.xtrans.promptpay.senderType = this.trans.debtor.info.atm_flag.get("2")
      result_mode: merge
      output: |-
            root = this
