processor_resources:
  - label: "{package}_c04e1793ee23ec08"
    hp_transform:
      name: "Init Loop"
      input: |-
            root = this
      mapping: |-
            root = this
            root.size = this.trn_dtl.length()
            root.index = 0
            root.trn_dtl_prep = []
            root.trn_dtl_str = []
            
      result_mode: merge
      output: |-
            root = this
