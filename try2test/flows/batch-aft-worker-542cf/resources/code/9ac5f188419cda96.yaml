processor_resources:
  - label: "{package}_9ac5f188419cda96"
    hp_transform:
      name: "Make SQL Command"
      input: |-
            root = this
      mapping: |-
            root = this
            let value = {
                            "cache_value": {
                            "sqlcmd": " select exdt,acn,rflg from rflgc where acn in ({}) and rflg = '11' ",
                            "refield": "exdt,acn,rflg"
                            }
                        }
                        
            root.strValue = $value.string()
      result_mode: merge
      output: |-
            root = this
