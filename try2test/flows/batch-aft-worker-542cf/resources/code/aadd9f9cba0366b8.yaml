processor_resources:
  - label: "{package}_aadd9f9cba0366b8"
    mongodb:
      name: "CreateAdvanceTransferLog"
      profile: |-
        {{hyper_ibank_mongo_config}}
      database: advance_transfer
      operation: insert-one
      collection: advance_transfer_log
      
      input: |-
        root = this
    
      document_map: |-
        root = this.data
        
      json_marshal_mode: relaxed
    
    
      result_mode: "merge"
    
      output: |-
        root = {}
        root.description = "Success"
        root.status = 0