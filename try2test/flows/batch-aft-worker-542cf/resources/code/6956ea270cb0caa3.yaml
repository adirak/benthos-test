processor_resources:
  - label: "{package}_6956ea270cb0caa3"
    hp_transform:
      name: "Set isOffus"
      input: |-
            root = this
      mapping: |-
            root = this
            root.isOffus = true
      result_mode: merge
      output: |-
            root = this
