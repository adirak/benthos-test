processor_resources:
  - label: "{package}_a82235fbf6c563f3"
    hp_transform:
      name: "Set Creditor AccountID"
      input: |-
            root = this
      mapping: |-
            root = this
            root.trans.creditor.AccountID = this.cr_acctno_int
      result_mode: merge
      output: |-
            root = this
