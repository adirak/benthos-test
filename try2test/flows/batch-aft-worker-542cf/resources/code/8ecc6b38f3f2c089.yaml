processor_resources:
  - label: "{package}_8ecc6b38f3f2c089"
    hp_transform:
      name: "Rename Value"
      input: |-
            root = this
      mapping: |-
            root = this
            root.redis_value = this.result
            
            root.expire = 0
            root.in_cache_value = true
      result_mode: merge
      output: |-
            root = this
