processor_resources:
  - label: "{package}_315eddd728cd9895"
    hp_transform:
      name: "Prepare to Call Query"
      input: |-
            root = this
      mapping: |-
            root = this
            root.sql_key = "MBTRNDPINF"
            root.conditions = ["#{acctId}"]
            root.params = { "acctId" : [0, json("trans."+action+".AccountID")] }
            
            root.mockupName = "account_trans_debtor"
            root.arrayType = false
      result_mode: merge
      output: |-
            root = this
