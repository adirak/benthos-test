processor_resources:
  - label: "{package}_3777c42eaf931e18"
    hp_transform:
      name: "Get CID"
      input: |-
            root = this
      mapping: |-
            root = this
            root.dr_acct = this.trans.debtor.AccountID
            root.cr_acct = this.trans.creditor.AccountID
      result_mode: merge
      output: |-
            root = this
