processor_resources:
  - label: "{package}_9f028b0b71d219a5"
    hp_transform:
      name: "Set Status"
      input: |-
            root = this
      mapping: |-
            root = this
            root.status.status = this.code
      result_mode: merge
      output: |-
            root = this
