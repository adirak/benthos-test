processor_resources:
  - label: "{package}_ab22bb51adaf2e96"
    hp_transform:
      name: "Set up param to query"
      input: |-
            root = this
      mapping: |-
            root = this
            root.params = {"cid":this.cid}
            root.conditions = null
            root.typeArray = true
            root.sql_key = "MBDEPTSTOP"
            root.mockupName = "stop_account"
            
      result_mode: merge
      output: |-
            root = this
