processor_resources:
  - label: "{package}_ba3375fcd9f02aa1"
    hp_transform:
      name: "Set Status to Code"
      input: |-
            root = this
      mapping: |-
            root = this
            root.status.status = code
            root.trans.creditor.status = code
            
      result_mode: merge
      output: |-
            root = this
