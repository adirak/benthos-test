processor_resources:
  - label: "{package}_cc36e516db83b5aa"
    hp_transform:
      name: "Increase Index"
      input: |-
            root = this
      mapping: |-
            root = this
            root.index = this.index + 1
      result_mode: merge
      output: |-
            root = this
