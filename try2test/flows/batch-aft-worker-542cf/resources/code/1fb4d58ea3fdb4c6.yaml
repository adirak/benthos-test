processor_resources:
  - label: "{package}_1fb4d58ea3fdb4c6"
    hp_transform:
      name: "Convert to Json"
      input: |-
            root = this
      mapping: |-
            root = this
            root.trans_cfg = this.strConfig.parse_json().or({})
      result_mode: merge
      output: |-
            root = this
