processor_resources:
  - label: "{package}_984fdf0a9de2e9c6"
    hp_transform:
      name: "Add dr_acct to cid"
      input: |-
            root = this
      mapping: |-
            root = this
            root.cid = this.cid.append(this.dr_acct)
      result_mode: merge
      output: |-
            root = this
