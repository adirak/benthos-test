processor_resources:
  - label: "{package}_795a4c48777d6638"
    hp_transform:
      name: "Read Record "
      input: |-
            root = this
      mapping: |-
            root = this
            let row = this.results.index(this.index)
            
            root.exp = if $row.get("EXP") != null {
                $row.get("EXP").or(timestamp_unix_milli())
            }else {
                $row.get("exp").or(timestamp_unix_milli())
            }
            
            root.acct = if $row.get("CID") != null {
                $row.get("CID").number(0).ceil().string()
            }else {
                $row.get("cid").number(0).ceil().string()
            }
            
            
            root.stp = if $row.get("STPWHY") != null {
                $row.get("STPWHY")
            } else {
                $row.get("stpwhy")
            }
      result_mode: merge
      output: |-
            root = this
