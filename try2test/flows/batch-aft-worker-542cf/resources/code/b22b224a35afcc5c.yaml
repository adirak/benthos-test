processor_resources:
  - label: "{package}_b22b224a35afcc5c"
    hp_transform:
      name: "Set Action and Request Data"
      input: |-
            root = this
      mapping: |-
            root = this
            root.action = "start"
            root.customerId = this.aft.customerID
            root.transTimes = this.aft.transTimes
            root.transRef = this.aft.transRef
            root.data = this.aft
            
      result_mode: merge
      output: |-
            root = this
