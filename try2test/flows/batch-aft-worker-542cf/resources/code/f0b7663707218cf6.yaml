processor_resources:
  - label: "{package}_f0b7663707218cf6"
    hp_transform:
      name: "Set Balance"
      input: |-
            root = this
      mapping: |-
            root = this
            root.result.balance = bal
            root.result.available = balavl
      result_mode: merge
      output: |-
            root = this
