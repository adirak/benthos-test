processor_resources:
  - label: "{package}_c0d82b429f741dc5"
    hp_transform:
      name: "Set dr_acct cr_acct"
      input: |-
            root = this
      mapping: |-
            root = this
            root.dr_acct = this.xchg_data.trans.debtor.AccountID
            root.cr_acct = this.xchg_data.trans.prepare.trans.creditor.info.promptpay.toAcctNo.number(0).floor()
      result_mode: merge
      output: |-
            root = this
