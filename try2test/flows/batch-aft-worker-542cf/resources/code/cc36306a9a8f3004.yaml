processor_resources:
  - label: "{package}_cc36306a9a8f3004"
    hp_transform:
      name: "Set Stop Acct"
      input: |-
            root = this
      mapping: |-
            root = this
            root.stop_acct = this.restrict
            root.accts = this.accts.append(result)
      result_mode: merge
      output: |-
            root = this
