processor_resources:
  - label: "{package}_78294c2cf30816ef"
    hp_transform:
      name: "Prepare Creditor transCode"
      input: |-
            root = this
      mapping: |-
            root = this
            root.trans.debtor.transReverse = this.trncde
            
            
            root.chanel = "MOBL"
            root.restict = "GLS"
            root.kind = 1
            
            root.trans_type = if this.trans_type == null || this.trans_type == "" {
                "TRANOFFUS"
            } 
      result_mode: merge
      output: |-
            root = this
