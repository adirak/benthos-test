processor_resources:
  - label: "{package}_6f0a071af1e646d0"
    hp_transform:
      name: "Get Account and Amt"
      input: |-
            root = this
      mapping: |-
            root = this
            root.account = this.transInput.glaccount.or(0)
            root.amt = this.ttxInput.amt
            
      result_mode: merge
      output: |-
            root = this
