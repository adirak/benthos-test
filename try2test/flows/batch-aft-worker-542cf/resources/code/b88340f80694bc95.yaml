processor_resources:
  - label: "{package}_b88340f80694bc95"
    hp_transform:
      name: "Set useDate to nextDate"
      input: |-
            root = this
      mapping: |-
            root = this
            root.nextDate = useDate
      result_mode: merge
      output: |-
            root = this
