processor_resources:
  - label: "{package}_143d2b8bbd30b550"
    hp_transform:
      name: "Make Key"
      input: |-
            root = this
      mapping: |-
            root = this
            root.redis_key = this.prefix + this.cif.index(this.index).string()
      result_mode: merge
      output: |-
            root = this
