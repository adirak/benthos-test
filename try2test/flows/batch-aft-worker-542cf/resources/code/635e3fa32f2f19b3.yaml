processor_resources:
  - label: "{package}_635e3fa32f2f19b3"
    hp_transform:
      name: "Rename to payment_info"
      input: |-
            root = this
      mapping: |-
            root = this
            root.payment_info = if not_found {
                {}
            } else {
                this.result
            }
      result_mode: merge
      output: |-
            root = this
