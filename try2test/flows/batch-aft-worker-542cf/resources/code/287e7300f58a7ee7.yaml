processor_resources:
  - label: "{package}_287e7300f58a7ee7"
    hp_transform:
      name: "Prepare to get trans config"
      input: |-
            root = this
      mapping: |-
            root = this
            root.chanel = "MOBL"
            root.restict = this.prod_cfg.rstgrp
            root.kind = -1
            root.branch = 99
            
            root.trans_type = if this.trans_type == null || this.trans_type == "" {
                "TRANOFFUS"
            }
            
            
      result_mode: merge
      output: |-
            root = this
