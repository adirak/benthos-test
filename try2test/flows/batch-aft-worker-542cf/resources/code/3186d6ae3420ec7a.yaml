processor_resources:
  - label: "{package}_3186d6ae3420ec7a"
    hp_transform:
      name: "Read Config in parm_val_key"
      input: |-
            root = this
      mapping: |-
            root = this
            root.strConfig = json("trans_cfg.cache_value."+parm_key+"."+parm_val_key)
      result_mode: merge
      output: |-
            root = this
