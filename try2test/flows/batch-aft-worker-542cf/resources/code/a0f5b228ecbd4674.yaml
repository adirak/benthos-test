processor_resources:
  - label: "{package}_a0f5b228ecbd4674"
    hp_transform:
      name: "hp_transform"
      input: |
          root = this
    
      mapping: |
          root = {}
          root.code = 0
          
          root.responseData = if this.result.response != "" {this.result.response.gcm_decode(this.eKey.string(), this.result.response.string(), -1).or({})}
          
      output: |
          let data = if this.responseData != null {
              this.responseData.parse_json()
          }
          root = $data.or({})
    
      result_mode: merge      