processor_resources:
  - label: "{package}_7f3da50787157e2c"
    hp_redis_cmd:
      profile: |-
        {{hyper_ibank_redis_config}}
      command: get
      args_mapping: root = [ "cbs_trans_ref_onus_" + this.trans.transRef.string().or("") ]
      input: |
        root = this
    
      result_mode: merge
    
      output: |
        root = {}
        root.trans_ref = this.parse_json().or({})