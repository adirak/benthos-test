processor_resources:
  - label: "{package}_a3023b7201d1203c"
    hp_transform:
      name: "Set Balance"
      input: |-
            root = this
      mapping: |-
            root = this
            root.result.balance = bal
            root.result.available = balavl
      result_mode: merge
      output: |-
            root = this
