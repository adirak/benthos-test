processor_resources:
  - label: "{package}_96cfe324d006eb59"
    hp_transform:
      name: "Set proxyId"
      input: |-
            root = this
      mapping: |-
            root = this
            root.xtrans.promptpay.proxyId = this.trans.creditor.Account
      result_mode: merge
      output: |-
            root = this
