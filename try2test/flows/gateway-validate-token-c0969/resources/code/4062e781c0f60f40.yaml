processor_resources:
  - label: "{package}_4062e781c0f60f40"
    hp_transform:
      name: "Set Default Error"
      inputs: |-
            root = this
      mapping: |-
            root = this
            root.status.description = "Error"
      result_mode: merge
      output: |-
            root = this
