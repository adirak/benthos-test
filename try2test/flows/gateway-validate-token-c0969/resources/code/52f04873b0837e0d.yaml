processor_resources:
  - label: "{package}_52f04873b0837e0d"
    hp_transform:
      name: "Decrypt encCustomerID"
      inputs: |-
            root = this
      mapping: |-
            root = this
            root.cif = this.encCustomerID
      result_mode: merge
      output: |-
            root = this
