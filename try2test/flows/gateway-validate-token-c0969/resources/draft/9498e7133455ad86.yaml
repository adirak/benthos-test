processor_resources:
  - label: "{package}_9498e7133455ad86"

    hp_transform:
      name: "Decrypt customerID"
      input: |-
        root = {}
      mapping: |-
        root = {}
      result_mode: merge
      output: |-
            let data =              {}
            let empty = {}
            root = $data.or($empty)
