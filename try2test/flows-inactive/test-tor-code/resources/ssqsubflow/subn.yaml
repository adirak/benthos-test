processor_resources:
  - label: subn
    hp_function:
      input: |-
        root = this
      processors:
        - hp_resource:
            id: subc
      output: |-
        root = root.copy_key(this)