# Recources
processor_resources:
  - label: "my_resource"
    hp_transform:
      name: "hp_transform"
      input: |
        root.B = "B"

      mapping: |
        root.C = "C"

      result_mode: "new"

      output: |
        root.D = "D" + this.X

      error: |
        root.F = "F"
