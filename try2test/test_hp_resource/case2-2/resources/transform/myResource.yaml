# Recources
processor_resources:
  - label: "my_resource"
    hp_transform:
      name: "hp_transform"

      mapping: |
        root.C = "C" + this.X

      result_mode: "void"

      error: |
        root.F = "F"
