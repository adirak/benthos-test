# Recources
processor_resources:
  - label: "my_resource"
    hp_transform:
      name: "hp_transform"

      mapping: |
        root.C = "C"

      error: |
        root.F = "F"

      result_mode: "void"
