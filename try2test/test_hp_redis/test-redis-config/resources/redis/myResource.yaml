# Recources
processor_resources:
  - label: "my_resource"
    hp_redis_cmd:
      profile: redis_config_79
      client_config: |-
        {{myredis_config}}
      command: get
      args_mapping: root = [ this.A ]
      input: |
        root = {}
        root.B = "Test_Key_2"

      result_mode: "new"

      output: |
        root = {}
        root.D = this
