processor_resources:
  - label: "{package}_d62ffa593cc59dda"
    hp_transform:
      name: "Error Code"
      inputs: |-
            root = this
      mapping: |-
            root = this
            root.C = this.A + this.X
      result_mode: merge
      output: |-
            root = this
