
processor_resources:
  - label: "{package}_ddb4338739792aca"
    hp_transform:
      name: "Define In/Out"
      input: |-
        root = this
      mapping: |-
        root = this
      result_mode: merge
      output: |-
        let filter = model2json("472a8ae2136c0969_ddb4338739792aca_in")
        root = this.json_filter($filter)
