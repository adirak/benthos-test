processor_resources:
  - label: "{package}_272e53e257af83de"
    hp_transform:
      name: "Return not_found "
      inputs: |-
            root = this
      mapping: |-
            root = this
            not_found = true
      result_mode: merge
      output: |-
            root = this
