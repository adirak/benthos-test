
processor_resources:
  - label: "{package}_a74b5755b718656d"
    hp_transform:
      name: "Define"
      input: |-
        root = this
      mapping: |-
        root = this
      result_mode: merge
      output: |-
        let filter = model2json("4bf82fdb553a38fc_a74b5755b718656d_in")
        root = this.json_filter($filter)
