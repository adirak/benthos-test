processor_resources:
  - label: "{package}_a3b0d4be2b64ed2b"
    hp_transform:
      name: "Code"
      inputs: |-
            root = this
      mapping: |-
            root = this
            root.response = "error"
      result_mode: merge
      output: |-
            root = this
