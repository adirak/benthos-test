processor_resources:
  - label: "{package}_9526fb98677b3fbe"
    hp_transform:
      name: "Convert String to Map"
      inputs: |-
            root = this
      mapping: |-
            root = this
            root.result = this.strResult.parse_json().or({})
      result_mode: merge
      output: |-
            root = this
