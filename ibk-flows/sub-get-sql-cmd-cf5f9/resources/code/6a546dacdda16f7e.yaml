processor_resources:
  - label: "{package}_6a546dacdda16f7e"
    hp_transform:
      name: "Make Key"
      inputs: |-
            root = this
      mapping: |-
            root = this
            redis_key = prefix + sql_key
      result_mode: merge
      output: |-
            root = this
